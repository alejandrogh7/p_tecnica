export const selectPopularMovies = (state) => state.movies.popularMovies;

export const selectMovieId = (state) => state.movies.movie;

export const selectSimilarMovies = (state) => state.movies.similarMovies;
